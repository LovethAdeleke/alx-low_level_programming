#include "main.h"
/**
 * _abs - function that computes the absolute
 * value of an integer.
 * @i: the integer
 * Return: the abs value of an integer
 */
int _abs(int i)
{
	if (i >= 0)
		return (i);
	else
		return (-i);
}
