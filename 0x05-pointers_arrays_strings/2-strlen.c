#include "main.h"
/**
 * _strlen - display the length of string
 * @s: string, pointer
 * Return: the length of string
 */
int _strlen(char *s)
{
	int p = 0;

	while (*(s + p) != 0)
	{
		P++;
	}
	return (p);
}
